services:
  moolias:
    build:
      context: .
    image: moolias:local
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${MOOLIAS_PORT:-8080}:8000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
